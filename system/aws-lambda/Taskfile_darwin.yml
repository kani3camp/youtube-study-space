version: '3'

tasks:
  build:
    platforms: [ darwin ]
    desc: Build a single lambda function
    cmds:
      - echo "Building function {{.CLI_ARGS}}"
      - GOARCH=amd64 GOOS=linux go build -o main {{.CLI_ARGS}}/main.go
      - zip main.zip main
