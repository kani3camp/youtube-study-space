<div
	class={`flex w-full flex-col items-center gap-3 px-6 py-8 text-center text-slate-200 ${((Astro.props as { className?: string; count?: number }).className ?? '')}`.trim()}
>
	<h2 class="text-2xl font-semibold uppercase tracking-[0.45em] text-slate-400">参加人数</h2>
	<div class="text-7xl font-black tracking-[0.1em] text-white">{(Astro.props as { count?: number }).count ?? 0}</div>
</div>
