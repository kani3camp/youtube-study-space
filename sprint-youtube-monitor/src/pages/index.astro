---
import Layout from '../layouts/Layout.astro';
import ClockDisplay from '../components/ClockDisplay.astro';
import PomodoroTimer from '../components/PomodoroTimer.astro';
import ParticipantsCount from '../components/ParticipantsCount.astro';

const pageTitle = 'Sprint YouTube Monitor';
const participantCount = 0;
const panelRadiusClass = 'rounded-[36px]';
---

<Layout>
	<main class="relative flex h-[1080px] justify-center bg-slate-950 p-16">
		<div class="flex w/full max-w-[1750px] gap-16">
			<section class={`flex-1 ${panelRadiusClass} border border-slate-800/80 bg-slate-900/70 p-12 shadow-[0_32px_120px_rgba(15,23,42,0.45)]`}>
				<header class="flex flex-col gap-4 text-left">
					<h1 class="text-4xl font-semibold text-white sm:text-5xl">{pageTitle}</h1>
					<p class="max-w-2xl text-base text-slate-200 sm:text-lg">
						YouTubeオンライン作業部屋 もくもくスプリント配信用画面
					</p>
				</header>
				<div class="mt-10 grid grid-cols-2 gap-8 text-sm text-slate-200">
					<div class={`space-y-3 ${panelRadiusClass} border border-slate-800/60 bg-slate-900/60 p-6`}>
						<h2 class="text-xs font-semibold uppercase tracking-[0.45em] text-slate-400">Sprint Notes</h2>
						<p class="leading-relaxed">
							配信中のメモや進捗ログを表示するプレースホルダーです。必要なウィジェットに差し替えることで、視聴者に状況を共有できます。
						</p>
					</div>
					<div class={`space-y-3 ${panelRadiusClass} border border-slate-800/60 bg-slate-900/60 p-6`}>
						<h2 class="text-xs font-semibold uppercase tracking-[0.45em] text-slate-400">Next Actions</h2>
						<ul class="space-y-2 pl-1">
							<li class="flex items-center gap-2">
								<span class="inline-flex h-2 w-2 rounded-full bg-emerald-400"></span>
								<span>配信開始前の接続確認</span>
							</li>
							<li class="flex items-center gap-2">
								<span class="inline-flex h-2 w-2 rounded-full bg-emerald-400"></span>
								<span>次のスプリント目標を設定</span>
							</li>
							<li class="flex items-center gap-2">
								<span class="inline-flex h-2 w-2 rounded-full bg-slate-500"></span>
								<span>完了タスクの共有</span>
							</li>
						</ul>
					</div>
				</div>
			</section>

			<div class="flex w-[480px] flex-col gap-10">
				<ClockDisplay align="center" class={`${panelRadiusClass} border border-slate-800/60 bg-slate-900/80 px-6 py-5 text-center text-slate-200 shadow-[0_18px_60px_rgba(15,23,42,0.35)]`} />
				<div class="flex flex-1">
					<PomodoroTimer class={`${panelRadiusClass} border border-slate-800/60 flex-1`} />
				</div>
				<ParticipantsCount count={participantCount} className={`${panelRadiusClass} border border-slate-800/60 bg-slate-900/70`} />
			</div>
		</div>
	</main>
</Layout>
